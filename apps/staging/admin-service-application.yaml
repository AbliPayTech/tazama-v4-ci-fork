apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: admin-service-staging
  namespace: argocd
spec:
  project: tazama
  source:
    repoURL: 'https://github.com/tazama-lf/cloud-infrastructure-deploy'
    targetRevision: dev
    path: k8s/overlays/staging/admin-service
    # Since kustomize is inferred by path ending in kustomization, no explicit kustomize block needed
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: tazama
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
